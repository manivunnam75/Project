using TrainnePortal.Models.User;

namespace TrainnePortal.Models.Services
{
    public interface Icrud
    {
      Registration AddTrainee(Registration r);
       // BatchModel AddBatch(BatchModel batch);  

    }
}
